# NAF-cdf

Use the Neural Autoregressive Flow (NAF) to model the cumulative distribution function (CDF) of a univariate distribution. More generally, it estimates the CDFs of each dimension of a random vector. 
